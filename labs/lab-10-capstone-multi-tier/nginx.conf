# proxy_connect_timeout: when the app tier is down, fail fast with a 504
# (about 2 s), as an ALB would, instead of hanging on NGINX's 60 s default.
events{} http{server{proxy_connect_timeout 2s; listen 80; location /api/{proxy_pass http://app:5000/;} location /{proxy_pass http://app:5000/;}}}