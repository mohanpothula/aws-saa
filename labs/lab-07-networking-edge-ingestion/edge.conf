events{} http{proxy_cache_path /tmp/cache keys_zone=edge:10m; server{listen 80; location /{proxy_cache edge; proxy_cache_valid 200 10m; add_header X-Cache $upstream_cache_status; proxy_pass http://origin;}}}